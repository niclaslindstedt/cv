User-agent: *
Allow: /

Sitemap: https://niclaslindstedt.se/sitemap.xml

# LLM-friendly entry points. The rendered site is also pre-rendered into the
# static HTML, but agents whose fetchers truncate the page should start here:
#
#   /llms.txt   — small markdown index (llmstxt.org convention)
#   /resume.json — full structured CV (bilingual en/sv); /cv.json is an alias
